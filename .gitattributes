# Normalise to LF in the repository. CI runs on Linux; development here is on
# Windows, and without this the two disagree about every line of every file.
* text=auto eol=lf

*.mmdb binary
*.png binary
*.ico binary
